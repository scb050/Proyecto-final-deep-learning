model:
  name: gru
  class: src.models.model_gru.GRUForecaster

architecture:
  hidden_size: 128
  num_layers: 2
  dropout: 0.2
  bidirectional: false

training:
  batch_size: 64
  epochs: 25
  lr: 1.0e-3
  optimizer: adam
  weight_decay: 1.0e-5
  early_stopping_patience: 8
  grad_clip: 1.0
